services:
  web:
    image: ghcr.io/pyobs/pyobs/pyobs-robotic-backend:latest
    command: >
      sh -c "
      uv run manage.py migrate &&
      uv run manage.py collectstatic --no-input &&
      uv run gunicorn -w 4 -b 0.0.0.0:8000 'pyobs_robotic_backend.wsgi'
      "
    volumes:
      - static_volume:/static
    env_file:
      - ./.env
    depends_on:
      - db
      - rabbitmq

  db:
    image: postgres:18
    volumes:
      - postgres_data:/var/lib/postgresql
    env_file:
      - ./.env

  nginx:
    image: nginx
    volumes:
      - static_volume:/static
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 8097:80
    depends_on:
      - web

  rabbitmq:
    image: rabbitmq:4.2
    restart: always
    environment:
      RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS: "-rabbitmq_deprecated_features permit transient_nonexcl_queues true"

  celery:
      image: ghcr.io/pyobs/pyobs/pyobs-robotic-backend:latest
      command: uv run celery -A pyobs_robotic_backend worker -B --loglevel INFO --without-gossip --without-mingle --without-heartbeat
      env_file:
        - ./.env
      depends_on:
        - db
        - rabbitmq
      restart: always
      ulimits:
        nofile:
          soft: 4096
          hard: 4096

  task_scheduler:
      image: ghcr.io/pyobs/pyobs/pyobs-robotic-backend:latest
      command: uv run manage.py runscript pyobs_robotic_backend.task_scheduler
      env_file:
        - ./.env
      depends_on:
        - db
        - rabbitmq
      restart: always

volumes:
  postgres_data:
  static_volume:
