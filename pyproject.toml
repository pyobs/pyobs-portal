[project]
name = "pyobs-robotic-backend"
version = "0.1.0"
description = "Backend for pyobs robotic software"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "astropy>=7.2.0",
    "django>=6.0.5",
    "pyobs-core>=1.41.0",
    "unicorn>=2.1.4",
]
